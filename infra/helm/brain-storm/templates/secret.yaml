{{/*
  This Secret is a template stub.
  In production, create secrets externally via AWS Secrets Manager, Vault, or
  kubectl create secret generic brain-storm-backend-secret \
    --from-literal=DATABASE_PASSWORD=<value> \
    --from-literal=JWT_SECRET=<value> \
    --from-literal=STELLAR_SECRET_KEY=<value>

  The chart references .Values.backend.existingSecret for all sensitive lookups.
  This file is intentionally empty so helm lint passes without exposing real values.
*/}}
